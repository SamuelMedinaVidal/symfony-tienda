<div class="pb-5">
    <div class="product-item position-relative bg-light d-flex flex-column text-center">
        <img class="img-fluid mb-4" 
             src="{{ asset('img/' ~ producttype.photo) }}" 
             alt="{{ producttype.name }}"
             style="height: 250px; object-fit: contain; padding: 20px;">
        
        <h6 class="text-uppercase">{{ producttype.name }}</h6>
        
        <h5 class="text-primary mb-0">{{ producttype.price|number_format(2, ',', '.') }} €</h5>
        
        <div class="btn-action d-flex justify-content-center">
            <a class="btn btn-primary py-2 px-3 add-to-cart" data-id="{{ producttype.id }}">
                <i class="bi bi-cart"></i>
            </a>

            <a class="btn btn-primary py-2 px-3 open-info-product" data-id="{{ producttype.id }}">
                <i class="bi bi-eye"></i>
            </a>
        </div>
    </div>
</div>